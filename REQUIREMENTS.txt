(int|real|bool)     TYPE
int X;
X = 1;
bool expression evaluation


type checking, type auto promotion (with no precision loss: NO int X = (real 1))

choercion (real -> int) (int -> bool) (int + int = int) (float + int = float)

print (1 1,1 true false)

int [3] array
int [3][4] array

array[1] = 3

print array()
